## minimum
### exercise
Строим генеалогическое дерево из файла excel. Напиши программу на Браузерном js, весь код в index.html.  
Рендерим схему в mermaid. При наличии специальной библиотеки mermaid для генеалогического дерева используем ее. Дерево должно быть похоже на www.myheritage.com  
При старте index.html данные считываются из tree.xlsx с листа person. Идентификаторами в mermaid являются idA, на схеме отображаются поля: label, birth, death. Дерево строится на основе предикатов hasFather и hasMother.  
Внизу схемы должна быть кнопка «mermaid.live», которая формирует ссылку и передает в https://mermaid.live/edit с использованием pako (https://mermaid.live/edit#pako).  
В папке .pic хранятся минифото для персонажей дерева в одноименном файле с idA и расширением .png. При отсутствии файла в качестве минифото подставляется dafaultm.png для мужчин (male, М,  см. поле sex) или dafaultf.png для женщин (female, Ж,  см. поле sex).

---
Мы должны написать программу на браузерном JS, весь код в одном index.html. Нужно прочитать данные из файла tree.xlsx (лист person), построить генеалогическое дерево с помощью mermaid, используя специальную библиотеку mermaid для генеалогического дерева (если есть). Но похоже, mermaid имеет базовую поддержку родословных через graph TD, но для более сложного дерева с семьями можно использовать специальный синтаксис? В mermaid есть диаграмма "familyTree"? Я знаю, что в mermaid есть "mindmap", "flowchart", "graph", но нет встроенной генеалогической. Однако есть возможность использовать кастомные диаграммы с помощью флоу чартов и специального синтаксиса. Но в задании сказано: "При наличии специальной библиотеки mermaid для генеалогического дерева используем ее". Возможно, имеется в виду mermaid-extension или типа того. Но проще построить дерево через flowchart, используя узлы для людей и связи родитель-ребенок. Также нужно отображать поля label, birth, death. Еще нужно добавить миниатюры фотографий из папки .pic с именем idA.png. При отсутствии использовать defaultm.png или defaultf.png в зависимости от пола.

Данные: в файле tree.xlsx лист person содержит столбцы: idA (формула =SUBSTITUTE(B2, " ", "_") - т.е. формируется из label заменой пробелов на подчеркивания), label, sex, newSurname, hasFather, hasMother, birth, death. В строках данные для 6 человек (начиная с B2). Остальные пустые. Нам нужно прочитать этот файл. Поскольку мы в браузере, мы должны дать пользователю возможность загрузить файл, либо встроить данные в код? Но условие: "При старте index.html данные считываются из tree.xlsx с листа person." Т.е. при открытии страницы она должна загрузить этот файл и прочитать. Но файл находится на сервере или локально? Обычно в таких заданиях подразумевается, что файл лежит рядом с index.html, и мы можем загрузить его через fetch. Но если открывать локально, то fetch может не работать из-за CORS, но можно использовать file input. Однако в условии "При старте index.html данные считываются из tree.xlsx", вероятно, предполагается, что мы можем загрузить файл через AJAX с сервера (например, если всё лежит в одной папке и открывается через localhost). Но для простоты можно добавить загрузку файла через input, но в условии сказано "считываются из tree.xlsx", т.е. автоматически. Лучше использовать fetch, но если файл локально, то сработает только если запускать через сервер (например, с помощью live server). Допустим, мы будем использовать fetch.

Для чтения xlsx в браузере нужна библиотека. Можно использовать SheetJS (xlsx) или аналоги. Мы можем включить CDN SheetJS для чтения файлов Excel. После загрузки файла парсим лист "person". Из данных строим узлы mermaid.

Построение дерева: на основе hasFather и hasMother. Каждый человек имеет idA (уникальный). hasFather и hasMother ссылаются на idA отца и матери. Нам нужно построить связи. Можно строить как flowchart, где каждый человек - узел с формой (прямоугольник, круг). В mermaid можно использовать стили, добавлять картинки? Согласно документации, можно добавлять изображения в узлы с помощью синтаксиса: node_id["https://image.jpg label"] но это экспериментально. Лучше использовать стандартные фигуры, но отобразить фото рядом? Задание: "В папке .pic хранятся минифото для персонажей дерева в одноименном файле с idA и расширением .png. При отсутствии файла в качестве минифото подставляется dafaultm.png для мужчин или dafaultf.png". То есть нужно отобразить фото внутри узла? В mermaid, наверное, можно вставить иконки через образы, но это сложно. Может быть, имеется в виду использовать библиотеку mermaid для построения дерева, но с кастомными HTML-узлами? Но mermaid не поддерживает HTML-содержимое узлов. Возможно, задание предполагает генерацию HTML-схемы, а mermaid используется только для визуализации графа. Но мы можем попытаться отобразить фото через эмбеддинг base64? Не уверен.

Альтернатива: использовать специализированную JS библиотеку для генеалогических деревьев, но сказано использовать mermaid. Может, есть mermaid-диаграмма "genealogy" или "familyTree" в виде плагина? Быстрый поиск: есть расширение mermaid для генеалогии под названием "mermaid-familytree"? Не знаю.

Проще построить дерево в виде flowchart, где каждый узел содержит текст: label, годы жизни (birth-death). И добавить маленькую иконку (фото) слева от текста с помощью эмодзи или Unicode? Не фото. Для фото нужен HTML, но mermaid не поддерживает.

Возможно, правильнее использовать не mermaid, а другую библиотеку? Но условие явно требует mermaid. Давайте поищем: в mermaid можно вставлять изображения с помощью "https://src" в тексте узла? Экспериментально: mermaid поддерживает вставку иконок FontAwesome, но не произвольных изображений. Однако можно использовать base64-encoded изображения в data URI? Можно попробовать. Но тогда все изображения нужно встроить в граф.

Лучше создать схему с помощью HTML-элемента и svg, но задание требует именно mermaid. Будем использовать mermaid, а фото не будем вставлять в узлы, а просто добавим легенду или что-то еще. Или сделаем отдельную галерею. Но условие говорит "минифото для персонажей дерева ... подставляется". То есть должно быть минифото в узле.

Другой вариант: использовать библиотеку "mermaid" в сочетании с "mermaid.cli", но это для рендеринга. Наверное, можно создать HTML-страницу, которая генерирует mermaid-код, а затем вставляет его в div с рендерингом mermaid. И для фото можно использовать иконки: если файл существует, то показывать символ фото, но не само фото.

Но вероятно, подразумевается, что мы генерируем схему в mermaid, а фото добавляем отдельно? Непонятно.

Давайте уточним: "В папке .pic хранятся минифото для персонажей дерева в одноименном файле с idA и расширением .png. При отсутствии файла в качестве минифото подставляется dafaultm.png для мужчин (male, М, см. поле sex) или dafaultf.png для женщин (female, Ж, см. поле sex)." Возможно, это для дополнительного отображения, например, при наведении или в отдельном блоке. Или мы можем генерировать HTML-элементы с фото и подписями, но mermaid здесь только для связей.

Может быть, можно использовать mermaid для отображения дерева, а фото выводить рядом с узлами через абсолютное позиционирование, но это сложно.

Я думаю, что стоит интерпретировать так: мы строим дерево с помощью mermaid, а фото используются для создания "аватарки" внутри узла, но поскольку mermaid не поддерживает картинки, мы можем попытаться использовать эмодзи или символы для обозначения пола. Но требование явное: "минифото". Поэтому, возможно, имеется в виду, что мы генерируем HTML-таблицу или сетку с фото и связями, а mermaid используем только для отображения связей? Но тогда зачем mermaid?

Другой вариант: использовать библиотеку "familytree" (например, https://github.com/BafS/George) но не mermaid.

Давайте перечитаем задание: "Строим генеалогическое дерево из файла excel. Напиши программу на Браузерном js, весь код в index.html. Рендерим схему в mermaid. При наличии специальной библиотеки mermaid для генеалогического дерева используем ее. Дерево должно быть похоже на www.myheritage.com При старте index.html данные считываются из tree.xlsx с листа person. Идентификаторами в mermaid являются idA, на схеме отображаются поля: label, birth, death. Дерево строится на основе предикатов hasFather и hasMother. Внизу схемы должна быть кнопка «mermaid.live», которая формирует ссылку и передает в https://mermaid.live/edit с использованием pako (https://mermaid.live/edit#pako). В папке .pic хранятся минифото для персонажей дерева в одноименном файле с idA и расширением .png. При отсутствии файла в качестве минифото подставляется dafaultm.png для мужчин (male, М, см. поле sex) или dafaultf.png для женщин (female, Ж, см. поле sex)."

То есть программа должна: загрузить Excel, построить дерево в mermaid (скорее всего, в виде графа), отобразить его на странице, и добавить кнопку для открытия этого же дерева в mermaid.live (сжато через pako). При этом в узлах должны быть поля label, birth, death. А фото - это отдельный элемент, который должен быть как-то связан с узлами. Может, фото должно отображаться внутри узла mermaid, но для этого можно использовать HTML-контейнеры с абсолютным позиционированием поверх svg. Но это сложно.

Возможно, ожидается, что мы используем "mermaid" в сочетании с "mermaid-example" или что-то типа "mermaid live edit" поддерживает вставку изображений? Нет, mermaid.live просто рендерит код.

Я думаю, что можно пойти на компромисс: отображать фото отдельно от mermaid-схемы, например, в виде списка или таблицы. Но это не соответствует заданию.

Еще идея: использовать библиотеку "mermaid" для построения дерева, но затем добавить кастомные элементы через JavaScript после рендеринга, вставив изображения в узлы SVG. Это возможно: после рендеринга mermaid создает SVG с элементами <g> для каждого узла. Можно найти соответствующий узел по id и добавить внутрь изображение. Но тогда нужно учитывать, что mermaid может перерисовываться, и мы должны делать это после каждого рендера. Это более реально. Мы можем после вызова mermaid.render или mermaid.init добавить код, который для каждого узла (person) найдет соответствующий элемент в SVG и вставит изображение в начало текста, либо заменит фон.

Но mermaid генерирует узлы как группы с прямоугольником и текстом. Текст находится внутри <text> элемента. Чтобы добавить изображение, нужно добавить <image> внутрь группы. Проблема: позиционирование. Можно попытаться поместить изображение слева от текста, но тогда текст нужно сдвинуть. Это сложно и будет выглядеть некрасиво, если изображение разного размера.

Альтернатива: использовать стилизованные узлы с иконками через шрифты, но не фото.

Исходя из реалий, вероятно, задание предполагает использование библиотеки "mermaid" и просто отображение фото в виде символов или подписей. Но упоминание папки .pic и defaultm.png/defaultf.png говорит о том, что нужно показывать именно изображения.

Возможно, есть библиотека "mermaid-genealogy", которая расширяет mermaid для поддержки фото. Я не знаю такой.

Другой подход: использовать не mermaid, а "d3.js" для построения дерева с фото. Но задание явно требует mermaid.

Взвесив всё, я предлагаю реализовать построение дерева в mermaid (flowchart) с узлами, содержащими текст (label, годы) и эмодзи, соответствующие полу (👨 или 👩). А фото выводить рядом с деревом в виде отдельной панели с миниатюрами, при клике на которые можно подсвечивать узел. Но это далеко от "минифото для персонажей дерева".

Может быть, я неправильно понимаю: возможно, под "минифото" имеется в виду, что в mermaid-диаграмме можно использовать иконки FontAwesome, а фото нужно преобразовать в base64 и вставить как data URI в атрибут src? Но mermaid не поддерживает HTML.

Я вижу, что есть способ использовать "click" события в mermaid, чтобы при клике на узел показывать фото. Но это не в узле.

Скорее всего, ожидается, что мы используем библиотеку "mermaid" для построения дерева, а фото не интегрируем в саму диаграмму, а просто выводим где-то рядом. Но тогда фраза "минифото для персонажей дерева" теряет смысл.

Я перечитаю: "В папке .pic хранятся минифото для персонажей дерева в одноименном файле с idA и расширением .png. При отсутствии файла в качестве минифото подставляется dafaultm.png для мужчин (male, М, см. поле sex) или dafaultf.png для женщин (female, Ж, см. поле sex)." Это выглядит как требование отображать фото в узлах. Если бы мы генерировали HTML-таблицу или древовидный список, это было бы легко. Но раз требуется mermaid, возможно, есть какой-то способ использовать кастомный рендеринг через mermaid API и добавлять элементы.

Я нашел в документации mermaid, что можно использовать "nodeFormatter" или "callback" после рендера, но это неофициально. Можно перехватить процесс рендера и модифицировать SVG.

Реализуем такой план:

Загружаем Excel, парсим данные.

Строим массив объектов Person: idA, label, sex, hasFather, hasMother, birth, death.

Для каждого определяем путь к фото: .pic/{idA}.png. Проверяем существование через fetch (HEAD запрос) или предполагаем, что файлы доступны. На практике, если файл не найден, используем defaultm/f.png (с тем же путем .pic/defaultm.png и .pic/defaultf.png).

Строим mermaid код для flowchart. Для каждого человека создаем узел с id = idA. Содержимое узла: метка, содержащая, возможно, HTML-подобный синтаксис? Можно использовать многострочный текст: "Имя Фамилия\n(1870-1924)". Но фото не вставить.

После рендеринга (используем mermaid.init или mermaid.run), для каждого узла находим его группу по id (в SVG у группы может быть id = "flowchart-"+idA). Затем внутри этой группы вставляем <image> в нужное место. Придется экспериментировать с координатами. Возможно, изображение можно добавить как отдельный элемент с преобразованием координат. Но тогда текст останется, и изображение будет поверх или рядом. Можно сдвинуть текст вправо, но это сложно.

Можно вообще заменить содержимое узла на HTML-подобный контент? Но mermaid генерирует SVG, и мы не можем просто вставить HTML. Однако можно создать <foreignObject> внутри SVG и вставить в него HTML с изображением и текстом. Это возможно! SVG поддерживает foreignObject для встраивания HTML. Мы можем заменить стандартный узел mermaid на custom? Но mermaid уже создает прямоугольник и текст. Лучше удалить их и добавить foreignObject.

Но это будет сложно и может сломаться при обновлении mermaid.

Я думаю, что более простой путь: использовать mermaid только для графа, а фото показывать в отдельной панели, возможно, в виде иконок рядом с узлами (после рендера). Но пользователь ожидает фото в узлах.

Я принимаю решение: в узлах mermaid мы будем отображать текст и небольшую иконку (символ) для фото, например, 📷. А сами фото будут выводиться в виде списка под деревом. Но это не удовлетворит требованию.

Давайте поищем, может, есть примеры mermaid с изображениями. Нашел: можно использовать стиль "img" для узлов, но это только для иконок FontAwesome. Для реальных изображений можно использовать data URI, но mermaid не поддерживает.

Я думаю, что лучше всего будет сгенерировать не mermaid-диаграмму, а HTML-дерево с помощью CSS и flex, но тогда кнопка "mermaid.live" не будет иметь смысла. Но кнопка нужна.

В конце концов, я думаю, что в задании допускается, что фото не будут внутри mermaid, а будут отображаться как всплывающие подсказки или дополнительная информация. Но тогда фраза "минифото для персонажей дерева" может означать, что мы должны показать фото рядом с именем в узле, но используя стандартные возможности mermaid, например, в виде эмодзи. А папка .pic может использоваться для каких-то других целей.

Я решил: сделаем mermaid-диаграмму с узлами, содержащими label, birth-death, и добавим emoji в зависимости от пола (👨/👩). А фото не будем вставлять, но добавим галерею под диаграммой, где будут миниатюры с именами. Это будет нашим компромиссом. Но явно не то.

Попробуем более изощренный способ: после рендера mermaid, мы можем найти все узлы, и для каждого добавить background-image через CSS? Но узлы - это группы, и мы можем применить стиль к прямоугольнику, но изображение будет масштабироваться и перекрывать текст. Можно попытаться добавить изображение как fill pattern? Сложно.

Я думаю, что лучше всего использовать библиотеку "mermaid" в сочетании с "mermaid-cli", но это не поможет.

Итак, напишем программу, которая:

Загружает tree.xlsx через fetch (или input).

Парсит лист person с помощью XLSX.

Строит mermaid код.

Отображает диаграмму.

Добавляет кнопку "mermaid.live", которая генерирует сжатую строку с помощью pako и открывает ссылку.

Для фото: после рендеринга, мы попытаемся для каждого узла добавить изображение слева от текста, но если не получится, то просто выведем фото отдельно. Но я попробую реализовать вставку изображений через foreignObject.

Алгоритм вставки:

Получить SVG элемент диаграммы.

Для каждого id человека, найти группу узла: обычно это <g> с id вида "flowchart-"+idA. Внутри есть <rect> и <text>.

Получить координаты и размеры прямоугольника.

Удалить или скрыть существующий текст и прямоугольник (или оставить, но мы сделаем свой).

Создать <foreignObject> с размерами прямоугольника, поместить внутрь HTML-структуру: <div> с display:flex, содержащий <img> и текст.

Вставить foreignObject в группу.

Это возможно, но нужно учесть, что mermaid может перерисовываться при изменении размера. Мы будем делать это после инициализации.

Также нужно проверять существование файлов изображений. Чтобы не делать множество запросов, можно попытаться загрузить изображение и в случае ошибки использовать default.

Итак, реализуем это.

Еще нюанс: idA генерируется как SUBSTITUTE(B2, " ", "_") в Excel. То есть в данных, которые мы получим, idA уже есть (формула, но при парсинге мы получим значения, так как Excel их вычисляет). В колонке idA будут значения типа "Ульянов_Владимир_Ильич" и т.д. hasFather/hasMother содержат такие же строки (например, "Ульянов_Илья_Николаевич").

Построение дерева: нужно создать связи от каждого человека к его родителям. Но в mermaid для графа мы можем указать направленные связи: child --> father и child --> mother. Но тогда получится много связей. Можно использовать стиль "родители-ребенок" с соединениями. Обычно в генеалогических деревьях показывают семьи: группируют родителей вместе и соединяют с детьми. В mermaid это можно сделать, создавая дополнительные узлы для семей, но это сложно. Упростим: будем просто соединять каждого ребенка с отцом и матерью. Это даст направленный граф сверху вниз (предки сверху). Чтобы было похоже на myheritage, нужно горизонтальное или вертикальное дерево. Можно использовать направление TB (top-bottom) или LR. Myheritage обычно сверху вниз: родители сверху, дети снизу. Мы используем TB.

Итак, код mermaid:
graph TB
id1["label1 (1870-1924)"]
id2["label2 (1831-1886)"]
id3["label3 (1835-1916)"]
id2 --> id1
id3 --> id1

Но тогда у нас не будет отображения супругов. У нас нет данных о браках, только родители. Можно добавить связи между родителями, если они оба указаны для ребенка. Но это не всегда так. Например, у Владимира указаны оба родителя. У Ильи Николаевича указаны оба родителя. У Марии Александровны родителей нет. Мы можем соединить Илью Николаевича и Марию Александровну? Нет явной связи. Обычно в генеалогическом дереве показывают брак. Можно создать дополнительный узел для брака или просто соединить родителей линией без стрелок. В mermaid для этого можно использовать пунктирные линии или просто соединение между ними. Например, добавить связь "husband --> wife" или наоборот, но это будет направленно. Лучше использовать стиль -- без стрелок. В mermaid есть синтаксис "id2 --- id3" для ненаправленной связи. Добавим такие связи между родителями, если они оба известны для хотя бы одного ребенка. Но если у человека несколько детей, родители должны быть связаны один раз. Можно собрать все пары родителей (husband+wife) и добавить связи между ними.

У нас нет явного списка семей. Но можно из данных: для каждого ребенка, если hasFather и hasMother не пусты, то отец и мать являются супругами. Таким образом, строим множество пар (father, mother). Затем для каждой такой пары добавляем связь "father --- mother" (ненаправленную). Это создаст горизонтальные связи между супругами.

Также нужно расположить родителей над детьми. В mermaid мы можем задать порядок с помощью подграфов? Но обычно алгоритм сам располагает.

Теперь о фото: после рендеринга будем модифицировать узлы.

Для проверки существования изображения: будем пытаться загрузить изображение через new Image() и слушать onload/onerror. Если onload, то используем его, иначе default. Но это асинхронно, и вставка будет после загрузки. Можно вставить изображение с событием onerror, чтобы заменить на default.

Мы создадим foreignObject внутри узла. Внутри foreignObject поместим HTML-структуру: <div style="display: flex; align-items: center; height: 100%; width: 100%; background: white; border-radius: 5px;"> <img src="..." style="width: 30px; height: 30px; object-fit: cover; margin: 2px;"> <span style="font-size: 12px;">Текст</span> </div>. Текст возьмем из исходного текста узла (можно извлечь из <text> или из данных).

Но нужно сохранить оригинальные размеры прямоугольника. Возможно, придется подогнать.

Альтернатива: не удалять оригинальный текст и прямоугольник, а добавить изображение поверх, но это будет наложение.

Я попробую реализовать замену.

Также нужно добавить кнопку mermaid.live. Для этого сформируем код диаграммы (без фото, только текст) и сожмем с помощью pako (deflate), затем base64url. Пример из документации mermaid.live: используется window.compressToEncodedURIComponent? Но они используют pako.deflate и потом base64url. Есть библиотека pako. Подключим её.

Итак, структура index.html:

Подключаем XLSX, mermaid, pako.

Элементы: input для загрузки файла (если fetch не сработает, дадим возможность загрузить вручную, но по условию "при старте данные считываются из tree.xlsx", поэтому можно попытаться fetch и если не получится, показать сообщение и предложить загрузить).

Контейнер для диаграммы.

Кнопка "mermaid.live".

Обработка: после загрузки данных строим код, инициализируем mermaid с theme: default или forest. Вызываем mermaid.run().

После рендера вызываем функцию enhanceWithPhotos, которая ищет все узлы с id.

Нюанс: mermaid присваивает id узлам в формате "flowchart-<idA>-<число>"? Я посмотрю: обычно для узла с id "person1" создается группа с id="flowchart-person1-1234"? Но может быть просто "person1". Лучше использовать селектор по тексту? Проще после рендера найти все узлы, которые соответствуют нашим id. Можно дать mermaid опцию secure: false, чтобы сохранить id. В документации: при использовании flowchart, идентификатор узла становится id элемента. По умолчанию, для узла с именем "A" создается элемент с id="flowchart-A-123". Но можно использовать "classDef" или "click" события, чтобы получить узел. Я думаю, можно использовать такой подход: добавить для каждого человека запись click: click idA callback. Тогда при клике можно получить элемент. Но нам нужно не это.

Мы можем после рендера получить все элементы с атрибутом data-id? Посмотрим структуру. На практике, mermaid создает группу с id="flowchart-"+idA+"-"+номер. Но если idA уникальны, то в конце добавляется счетчик. Трудно предсказать. Можно использовать селектор по тексту, но текст может быть изменен.

Проще добавить в код mermaid для каждого узла атрибут class с idA, например, classDef person fill:#fff; а затем для узла добавить class person. Но как добавить класс? В mermaid можно задать класс для узла: class idA person; тогда в SVG у группы будет класс "person". Это позволит нам выбрать все группы с этим классом. И затем внутри группы найти элемент <title> или <desc> с исходным idA? Или использовать data-атрибут.

Я сделаю так: в конце mermaid кода добавлю для каждого человека строку "class " + idA + " person;". Затем после рендера выберу все элементы с классом "person". Для каждого такого элемента получу его id (группы) или какой-то атрибут, чтобы сопоставить с человеком. Можно сохранить массив personById, а затем для каждой группы искать соответствие по тексту? Не надежно.

Лучше: в группе есть элемент <title> с текстом узла (исходный ID?). Посмотрим: обычно mermaid генерирует <title> с содержимым узла (то, что внутри скобок). В нашем случае это будет "label (birth-death)". Не совпадает с idA. Не подходит.

Есть другой способ: добавить в узел дополнительный атрибут через синтаксис mermaid? Не знаю.

Можно при рендере перехватить сгенерированный SVG и проанализировать его. Или использовать mermaid API "mermaid.parse" и "mermaid.render" с получением SVG строки, затем вставить ее вручную и потом модифицировать. Так мы получим полный контроль. Я предпочту использовать mermaid.render, которая возвращает SVG строку. Затем вставим её в контейнер, и затем уже обработаем с помощью DOM парсера. При этом мы можем модифицировать узлы до вставки или после. Мы можем в SVG строке найти все <g> с id, соответствующими нашим idA? Но id в SVG будут содержать суффиксы. Однако можно по окончанию строки? Не факт.

Более простой путь: после вставки SVG через innerHTML, используем querySelectorAll('g[class*="person"]')? Но мы добавили класс person для каждого узла, значит, у групп будет класс "person". Тогда мы можем найти их все. Но чтобы сопоставить каждую группу с конкретным idA, нам нужен какой-то идентификатор. Мы можем в узле mermaid добавить текст, содержащий idA, скрытый? Например, добавить в узел невидимый текст с idA. Это можно сделать, включив idA в метку узла, но он будет виден. Или мы можем использовать атрибут data-id в SVG через шаблон? Но mermaid не позволяет добавлять произвольные атрибуты.

Другой вариант: после рендера мы можем прочитать текст внутри узла (он внутри <text>), и сопоставить с данными по label. Но у разных людей могут быть одинаковые имена? В нашем примере все уникальны. Можно сопоставить по полному имени и годам. Но если года совпадут, будут проблемы. Но вероятность мала.

Я думаю, мы можем для каждого человека после рендера искать группу, внутри которой есть <text> содержащий его label и годы. Для этого нужно сформировать ожидаемую строку. Это будет работать, если mermaid не разобьет текст на несколько элементов. Обычно текст цельный. Итак, будем искать по содержимому.

После нахождения группы, мы будем ее модифицировать: удалим все дочерние элементы (rect, text) и вставим foreignObject.

Это довольно сложно, но выполнимо.

Итак, приступим к написанию кода.

План действий в index.html:

Подключить библиотеки: xlsx.full.min.js, mermaid, pako.

HTML: заголовок, кнопка загрузки (на случай если fetch не сработает), контейнер для диаграммы, кнопка "mermaid.live".

JS:

Определить функцию loadData() для загрузки и парсинга файла.

При успехе, вызвать buildMermaid(people).

Функция buildMermaid строит код диаграммы: собирает все узлы (для каждого человека строка: idA["метка (birth-death)"]) + связи child->father, child->mother + связи супругов father --- mother (уникальные).

Добавляем классы: class idA person;

Вызываем mermaid.render('graphDiv', code, callback) или используем mermaid.init. Удобнее использовать mermaid.render, так как мы получаем svg строку и можем ее вставить.

После вставки svg, вызываем функцию enhanceWithPhotos(svgContainer, people).

В enhanceWithPhotos: для каждого человека, ищем группу по тексту. Для этого перебираем все элементы 'g' внутри svg, у которых есть дочерний 'text'. Получаем текст, обрезаем лишнее. Сравниваем с ожидаемой строкой: label + " (" + birth + "-" + death + ")" (если death пусто, то только birth). Учитываем, что birth/death могут быть пусты.

Если группа найдена, то:

Получаем координаты прямоугольника (rect) внутри группы (его ширину и высоту, x, y). Можно использовать getBBox, но лучше взять атрибуты rect.

Удаляем rect и text.

Создаем foreignObject с атрибутами x, y, width, height.

Внутрь помещаем div с CSS для отображения.

Добавляем img с src, обрабатывая ошибки загрузки.

Для фото: src = .pic/${idA}.png?${Date.now()} (чтобы избежать кэша при проверке). Если изображение не загружается, заменяем на .pic/default${sex === 'М' ? 'm' : 'f'}.png. Это можно сделать через onerror на img.

После добавления foreignObject, группа будет содержать наш элемент.

Кнопка "mermaid.live": берем текущий код диаграммы (без изменений фото), сжимаем pako.deflate, затем base64url и открываем ссылку.

Теперь детали:

Парсинг Excel: используем XLSX.read(data, {type: 'binary' или 'array'}). При fetch получаем arrayBuffer.

Лист 'person' может быть первым. Получим диапазон данных.

Столбцы: A:idA, B:label, C:sex, D:newSurname, E:hasFather, F:hasMother, G:birth, H:death. Нужно пропустить первую строку (заголовки). Но в данных строка 1 это заголовки? В таблице видим: | idA | label | sex | newSurname | hasFather | hasMother | birth | death |. Это заголовки. Данные начинаются со строки 2. Значит, будем читать начиная со 2 строки до последней заполненной.

Обработка пустых ячеек: hasFather, hasMother могут быть пустыми строками. birth/death могут быть пустыми. В Excel они представлены как числа или строки. Преобразуем в строки.

Создаем массив people.

Построение связей:
Для каждого человека, если hasFather не пусто, добавляем ребро: hasFather --> idA.
Если hasMother не пусто, добавляем ребро: hasMother --> idA.
Также собираем пары (father, mother) для добавления супружеских связей. Используем Set с ключом father+'_'+mother (упорядоченным). Затем для каждой пары добавляем связь father --- mother (ненаправленную).

Узлы: для каждого человека, метка: ${label} (${birth || '?'}-${death || '?'}). Если birth и death пусты, то просто label.

Код диаграммы:
graph TB
node1["метка1"]
node2["метка2"]
...
node1 --> node2
...
node3 --- node4
...
class node1 person;
class node2 person;
...

Для mermaid.render нужно указать контейнер или получить строку. Удобно: mermaid.render('mermaid-code', code, (svgCode) => { container.innerHTML = svgCode; then enhance...}). Но mermaid.render устарел? В новой версии используют mermaid.render('id', code, {…}). Но мы используем mermaid.run? Я проверю.

Лучше использовать mermaid.mermaidAPI.render('mermaidDiv', code, cb). Но в новых версиях mermaid (10+) есть mermaid.render. Я подключу последнюю версию.

Для простоты, я использую mermaid.init с элементом, но тогда не получу контроль над SVG до вставки. Но можно после init вызвать enhance.

Итак, я выберу mermaid.init({ theme: 'default' }, '.mermaid'), а затем после того, как mermaid завершит рендеринг (можно использовать mermaid.parseError или наблюдать MutationObserver), но проще использовать setTimeout с проверкой. Ненадежно.

Лучше использовать асинхронный рендеринг с промисом: mermaid.render('mermaidDiv', code).then(svg => {...}). В версии 10+ это работает.

Итак, в функции buildMermaid:
const code = ...;
mermaid.render('mermaidDiv', code).then((svg) => {
document.getElementById('mermaidContainer').innerHTML = svg;
enhanceWithPhotos();
});

Теперь enhanceWithPhotos:
const svg = document.querySelector('#mermaidContainer svg');
if (!svg) return;
people.forEach(person => {
const expectedText = ${person.label} (${person.birth || '?'}-${person.death || '?'});
// ищем группу, содержащую текст
const groups = svg.querySelectorAll('g');
let targetGroup = null;
for (let g of groups) {
const textElem = g.querySelector('text');
if (textElem && textElem.textContent.includes(expectedText)) { // возможно точное совпадение
targetGroup = g;
break;
}
}
if (!targetGroup) return;
// получаем rect
const rect = targetGroup.querySelector('rect');
if (!rect) return;
const x = parseFloat(rect.getAttribute('x'));
const y = parseFloat(rect.getAttribute('y'));
const w = parseFloat(rect.getAttribute('width'));
const h = parseFloat(rect.getAttribute('height'));
// удаляем rect и все text
targetGroup.querySelectorAll('rect, text').forEach(el => el.remove());
// создаем foreignObject
const fo = document.createElementNS('http://www.w3.org/2000/svg', 'foreignObject');
fo.setAttribute('x', x);
fo.setAttribute('y', y);
fo.setAttribute('width', w);
fo.setAttribute('height', h);
// HTML внутрь
const div = document.createElement('div');
div.style.cssText = 'display: flex; align-items: center; height: 100%; width: 100%; background-color: #f0f0f0; border-radius: 4px; overflow: hidden;';
const img = document.createElement('img');
img.style.cssText = 'width: 30px; height: 30px; object-fit: cover; margin: 2px; border-radius: 3px;';
img.src = .pic/${person.idA}.png?t=${Date.now()};
img.onerror = function() {
this.onerror = null;
this.src = .pic/default${person.sex === 'М' ? 'm' : 'f'}.png?t=${Date.now()};
};
const span = document.createElement('span');
span.style.cssText = 'font-size: 12px; line-height: 1.2; margin-left: 2px;';
span.textContent = expectedText;
div.appendChild(img);
div.appendChild(span);
fo.appendChild(div);
targetGroup.appendChild(fo);
});

Но это вставит foreignObject, но исходные rect и text удалены. Убедимся, что группа не имеет лишних элементов.

Также возможно, что mermaid добавляет дополнительные элементы (например, для подсветки). Но мы удалим только rect и text. Оставим title, если есть.

Теперь о супружеских связях. Если мы добавили связь father --- mother, то она будет выглядеть как линия между узлами. Но узлы теперь с foreignObject, линия останется. Хорошо.

Теперь кнопка mermaid.live: генерируем код такой же, как использовали для рендера (без фото), затем используем pako.deflate для сжатия, затем btoa? Но mermaid.live использует base64url с заменой символов. Есть функция compressToEncodedURIComponent из lz-string, но они используют pako. В документации mermaid.live: https://mermaid.live/edit#pako:... где после #pako: идет сжатая строка. Сжатие: pako.deflateRaw(str, {level: 9})? Надо посмотреть. Я посмотрю исходники mermaid.live. Наверное, они используют window.btoa(pako.deflate(unescape(encodeURIComponent(str)), { to: 'string' })). Но точнее: pako.deflate(str) возвращает Uint8Array, затем преобразовать в строку с помощью String.fromCharCode.apply, затем btoa. И потом заменить + на -, / на _, = на .? В URL base64url обычно заменяют + на -, / на _, и удаляют =. Но mermaid.live использует стандартный base64 с символами +/ и =, но в фрагменте #pako: они используют url-safe? Я видел примеры: https://mermaid.live/edit#pako:eNpVUUtugzAQvQryCssLSFulkiLBIhKSHZHYgGnjr3pMiQi4e21Mo8Bs5s3nzcwzW7RCMRxQq2acUUZb7O0d-hE7cKqBdE7T3rS7nnQ1LeII8gBS9a3OK85LPU9op7DpFHD6e_-dUvL0Ci7R__3hJd47r9Ww9QjWX_T_UdL5kuZfZlOVLk7UKqNc8xx-FMvF6Uz_nCGeY3ZGw5LpQ1IMk5nLZIZiSuK3-GPp_y0z5w3pWXoTLVJ3I5ANhA1A7GjR1ZGo1taG2llxGqH1Wmv0H2P1bH0. Видно символы / и +. Так что просто btoa сжатой строки подойдет. Но нужно правильно подготовить строку.

Сделаем:
function compress(code) {
const data = new TextEncoder().encode(code);
const compressed = pako.deflate(data, { level: 9 });
return btoa(String.fromCharCode(...compressed));
}
const compressed = compress(mermaidCode);
const url = 'https://mermaid.live/edit#pako:' + compressed;

Это должно работать.

Теперь нужно обработать случай, когда birth или death пустые. В данных они могут быть числами, преобразуем в строку. Если нет, ставим '?'.

Также пол: в данных 'М' или 'Ж' (русские буквы). Учтем.

Проверка на наличие файлов: через img onerror.

Примечание: папка .pic должна быть относительно index.html. Файлы defaultm.png и defaultf.png должны там лежать. Создадим заглушки.

Теперь о чтении файла. Если fetch не сработает из-за CORS (локальный файл), мы можем показать input. Я добавлю кнопку "Загрузить tree.xlsx" для ручной загрузки.

Для fetch, указываем путь 'tree.xlsx' (относительный). Если fetch не удался, показываем сообщение и предлагаем выбрать файл.

Итак, напишем код.

Важно: в колонке idA уже есть значения, сгенерированные формулой. При парсинге мы получим эти строки (например, "Ульянов_Владимир_Ильич"). hasFather/hasMother содержат такие же строки.

Теперь напишем код.

Проверим, есть ли библиотека mermaid для генеалогии? Не будем усложнять.

Учтем, что mermaid может иметь лимит на количество узлов, но у нас мало.

Итоговый index.html будет содержать стили для контейнера.

Я напишу полный код с комментариями.

Также нужно включить мета-тег viewport и базовые стили.

Приступим.

