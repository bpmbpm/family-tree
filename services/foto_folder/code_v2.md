Замечания: Выдает:  Нет значений в столбце "idA" (нечего проверять), видимо потому, что значения в столбце "idA" задаются формулой. Скорректируй алгоритм.  
Доработай код с учетом того, что в локальном варианте запуска service_foto.html нет локального веб-сервер, но пользователь подтверждает открытие файла (для устранения проблем с CORS).

Мы получили замечания:

Проблема: "Нет значений в столбце 'idA' (нечего проверять)" возможно из-за того, что значения в столбце задаются формулой. При использовании sheet_to_json с header:1, вероятно, читаются только значения, но если в ячейке формула, то может быть пусто? Нужно убедиться, что мы получаем вычисленные значения. В XLSX.read мы получаем workbook с raw: false по умолчанию? Надо проверить. В XLSX.read по умолчанию raw: false, но формулы не вычисляются, они представлены как объекты. В нашем коде мы используем sheet_to_json с header:1, но она возвращает сырые данные, для формул это будет объект с формулой? Нужно использовать опцию raw: false? Или для получения вычисленного значения нужно включить опцию cellFormula: false? Вообще, чтобы получить значения формул, нужно, чтобы библиотека их вычислила. По умолчанию XLSX.read не вычисляет формулы, если не указать опцию sheetStubs или cellFormula. Для получения вычисленных значений лучше использовать опцию cellText: true? Или после чтения использовать функцию XLSX.utils.sheet_to_json с параметром raw: false? По умолчанию raw = false возвращает форматированные значения (текст), но для формул это будет результат вычисления, если библиотека поддерживает? На самом деле, библиотека SheetJS (xlsx) не выполняет вычисление формул автоматически, она просто возвращает формулу в виде строки. Чтобы получить вычисленное значение, нужно использовать опцию cellFormula: false? Или использовать функцию sheet_to_json с header:1 и затем обрабатывать каждую ячейку, проверяя, есть ли у неё поле f (формула) и извлекать из v (значение). Если в ячейке формула, то v может содержать результат, если он был сохранён в файле. Excel хранит последнее вычисленное значение. Так что просто используя v, мы получим значение, если файл был сохранён с этими значениями. Так что проблема может быть не в формулах, а в том, что в файле действительно нет значений, либо мы неправильно ищем столбец.

Но автор пишет: "видимо потому, что значения в столбце 'idA' задаются формулой". Возможно, в ячейках формула, и при преобразовании мы получаем объект формулы, а не строку. Чтобы получить отображаемое значение, нужно использовать cell.w или cell.v. В sheet_to_json с header:1, для каждой ячейки возвращается либо значение (если это просто строка/число), либо объект, содержащий свойства ячейки. Так что нам нужно при обработке строк проверять тип и извлекать текстовое представление.

Исправим: будем читать лист как массив массивов с опцией raw: false? sheet_to_json с параметром header: 1 возвращает массив строк, где каждая ячейка - это либо значение, либо объект с полями. По умолчанию используется raw: false, что означает, что возвращаются форматированные значения (т.е. если ячейка содержит формулу, то возвращается вычисленное значение, если оно доступно). Но если вычисленного значения нет, то может быть null? Возможно, что для формул возвращается объект, содержащий формулу. Надо уточнить. В документации: raw (default false) - если true, возвращает сырые значения (числа, строки, даты как числа), если false, возвращает отформатированный текст. Для формул это может не сработать.

Более надёжно: вручную пройти по строкам, используя XLSX.utils.sheet_to_json(worksheet, { header: 1, defval: '' }), но всё равно для формул результат будет формула? Может быть, использовать опцию raw: false и затем проверять тип ячейки: если это объект, то извлечь w (отформатированный текст) или v (значение). Либо использовать XLSX.utils.sheet_to_json(worksheet, { header: 1, raw: false }) и затем обработать каждую ячейку, приведя к строке.

Другой способ: использовать sheet_to_json с опцией header: 1 и затем обработать каждый элемент, приведя к строке: String(cell || '') или cell ? cell.toString() : ''. Но если cell - объект с формулой, то toString даст что-то типа "[object Object]". Поэтому нужно явно извлекать отображаемое значение: если cell и cell.w, то использовать cell.w, иначе если cell и cell.v, то cell.v, иначе cell.

Но в нашем коде мы просто берем row[idAColIndex] и вызываем .toString().trim(). Если это объект формулы, то toString вернет что-то не то. Исправим, добавив функцию получения значения ячейки.

Второе замечание: в локальном варианте без веб-сервера, при открытии через file://, браузер блокирует fetch из-за CORS. Пользователь может подтвердить открытие файла? Это не совсем так: в современных браузерах при открытии HTML с локального диска через file://, политика CORS запрещает загрузку других файлов через fetch, даже если они лежат рядом. Единственный способ - либо использовать локальный веб-сервер, либо разрешить доступ к файлам через специальные настройки браузера (например, в Chrome можно запустить с флагом --allow-file-access-from-files). Пользователь не может просто "подтвердить" открытие файла. Однако автор просит учесть, что пользователь подтверждает открытие файла для устранения проблем с CORS. Вероятно, имеется в виду, что в локальном режиме можно попытаться использовать другой подход, например, через выбор файла через <input type="file">, но это для загрузки самого Excel, а не для проверки файлов в папке. Либо предложить использовать упрощённый режим с ручным указанием путей?

Но в задании сказано: "Код должен быть браузерный JS и работать как при размещении service_foto.html на github pages, так и на локальном компьютере." То есть должно работать в обоих окружениях. На локальном компьютере без сервера открытие file:// не позволит делать fetch к другим локальным файлам. Значит, нужно либо требовать локальный сервер, либо использовать другой механизм для локального режима. Например, можно попросить пользователя выбрать папку с помощью <input type="file" webkitdirectory> и затем уже анализировать файлы, но это сложно и не соответствует исходной задумке (проверка по заранее заданной структуре). Возможно, автор ожидает, что при локальном открытии через file:// браузер всё же позволит загружать файлы из той же папки? Проверим: современные браузеры блокируют cross-origin запросы к файлам из file://, даже если они из той же директории, потому что источником считается file://, а запрос к другому файлу тоже file://, но с другим путём - это считается другой источник? Нет, все file:// считаются одним источником? На самом деле, в некоторых браузерах file:// считается "уникальным" источником, и политика CORS не применяется, но доступ к другим файлам может быть разрешён. Однако из-за ограничений безопасности, многие браузеры блокируют чтение файлов через fetch из file://, если страница также открыта через file://. В Chrome, например, это запрещено по умолчанию, нужно запускать с флагом --allow-file-access-from-files. Firefox тоже блокирует. Так что надёжного способа нет.

Поэтому мы можем добавить проверку: если страница открыта через file://, выводить предупреждение и предлагать пользователю либо использовать локальный сервер, либо вручную подтвердить (с помощью инструкции). Также можно попытаться использовать XMLHttpRequest с file://, но это также блокируется.

Второй момент: для проверки наличия файлов мы используем fetch с HEAD. В локальном режиме это работать не будет, если браузер не разрешает. Значит, для локального режима нужно предложить альтернативный способ: например, использовать <input type="file"> для выбора папки (с атрибутом webkitdirectory) и затем сравнивать имена файлов. Но это сложнее и требует дополнительного кода. Автор просит лишь "скорректируй алгоритм" для формул и "доработай код с учетом того, что в локальном варианте запуска service_foto.html нет локального веб-сервера, но пользователь подтверждает открытие файла (для устранения проблем с CORS)". Возможно, имеется в виду, что пользователь может открыть файл через диалог выбора, и мы сможем работать с ним через File API? Но у нас же не один файл, а много в папках.

Попробуем интерпретировать: пользователь "подтверждает открытие файла" — возможно, он может выбрать файл tree.xlsx через input, и тогда мы получим доступ к нему через FileReader, а для проверки файлов в папках мы можем использовать тот же подход с fetch? Нет, fetch не будет работать. Может, можно использовать API доступа к файловой системе (File System Access API), но он поддерживается не везде и требует подтверждения пользователя для каждого файла. Слишком сложно.

Другой вариант: для локального режима предложить использовать простой скрипт на Node.js, но задача именно браузерный JS.

Может быть, автор предполагает, что пользователь запускает HTML через локальный веб-сервер (например, python -m http.server) и поэтому проблем с CORS не будет. Тогда второе замечание неактуально? Но он явно пишет про локальный вариант без сервера.

Давайте уточним у автора? Невозможно. Будем действовать в рамках разумного: добавим проверку протокола: если window.location.protocol === 'file:', то выводим сообщение, что для работы с локальными файлами необходимо запустить локальный веб-сервер или разрешить доступ к файлам в браузере (инструкция). Также можно предложить пользователю вручную подтвердить открытие файлов через диалог выбора папки? Но это сложно.

Поскольку мы не можем полностью решить проблему CORS в file:// без дополнительных разрешений, добавим информационное предупреждение и инструкцию, как запустить локальный сервер. Это будет разумным компромиссом.

Итак, план доработок:

Исправить чтение ячеек с формулами: при получении значения idA из строки, обрабатывать ячейку, извлекая отображаемое значение (cell.w, cell.v, cell, или пустую строку). Добавить функцию getCellValue(cell).

Добавить проверку протокола file: и выводить предупреждение с инструкцией по запуску локального сервера.

Улучшить обработку ошибок.

Напишем обновлённый код.
