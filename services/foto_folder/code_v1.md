### service_foto
Составить файл service_foto.html. В файле service_foto.html задается список (JSON) папок \ листов excel. Программа открывает указанный в JSON блоке файл (из текущего каталога) и делает проверку:
С указанного в списке листа, например, foto_person, считывается значение поля idA – имя файла. Если в одноименной с названием листа папке (например, \foto_person) нет указанного файла (как значение в поле idA), то дается соответствующее предупреждение (в итоговом отчете по всем папкам \ листам).
Если в данной папке есть другие файлы, не указанные в поле idA для данной папки (имя листа совпадает с именем папки), то также видается предупреждение. 
Код должен быть браузерный JS и работать как при размещении service_foto.html на github pages, так и на локальном компьютере. 
Используй <script src="https://cdn.sheetjs.com/xlsx-0.20.2/package/dist/xlsx.full.min.js"></script>
Введи в JSON блок настраиваемые параметры: tree.xlsx и список его полей \ папок с файлами: 
foto_person
foto_family
foto_group
foto_location
В коде дай комментарии на русском.

### Think
Мы должны создать HTML-файл service_foto.html, который выполняет проверку файлов в папках на основе данных из Excel (xlsx) файла. Пользовательский интерфейс: есть JSON блок с настраиваемыми параметрами: путь к файлу Excel (tree.xlsx) и список листов (папок): foto_person, foto_family, foto_group, foto_location.

Программа открывает указанный файл Excel (из текущего каталога). Для каждого листа из списка считывает значения поля idA (имя файла). Затем проверяет наличие этих файлов в одноименной папке (например, "foto_person"). Если в папке нет файла, указанного в idA, выдает предупреждение. Если в папке есть другие файлы, не указанные в idA, тоже выдает предупреждение.

Требования: браузерный JS, должен работать на GitHub Pages и локально (поскольку доступ к файловой системе ограничен, то "текущий каталог" для GitHub Pages означает, что файлы должны быть доступны относительно URL. Для локальной работы нужно будет поместить все файлы в одну папку с HTML и открывать через file://, но тогда браузер может блокировать загрузку из-за CORS? Но мы используем File API для загрузки Excel? Сказано "программа открывает указанный в JSON блоке файл (из текущего каталога)" - вероятно, предполагается, что файл лежит рядом с HTML, и мы можем его загрузить через fetch. На GitHub Pages fetch работает для относительных путей. Для локального file:// fetch не будет работать из-за CORS, если не запускать сервер. Но обычно можно использовать file:// и fetch? Современные браузеры блокируют cross-origin requests для file://. Поэтому нужно либо использовать FileReader для выбора файла пользователем, либо использовать локальный сервер. Но задание конкретно: "должен работать как при размещении service_foto.html на github pages, так и на локальном компьютере." Вероятно, подразумевается, что на локальном компьютере файл открывается через file://, и мы должны использовать какой-то обходной путь, например, использовать XMLHttpRequest с file://? Но это небезопасно и обычно блокируется. Возможно, предполагается, что пользователь будет запускать локальный сервер, либо просто тестировать на GitHub Pages. Другой вариант: для локальной работы можно использовать File API, чтобы пользователь сам выбрал файл Excel. Но задание говорит: "В файле service_foto.html задается список (JSON) папок \ листов excel. Программа открывает указанный в JSON блоке файл (из текущего каталога)". То есть файл предопределен в JSON, и программа должна его открыть автоматически. Это возможно только если страница загружена через HTTP (локальный сервер или GitHub Pages). Тогда fetch с относительным путем сработает.

Итак, предположим, что мы используем fetch для загрузки tree.xlsx. На GitHub Pages это сработает, если файл лежит рядом. На локальной машине, если открыть через file://, fetch не сработает. Но можно запустить простой HTTP сервер: python -m http.server. Вероятно, под "на локальном компьютере" подразумевается такой же подход, как на GitHub Pages: файлы располагаются рядом, и открытие через HTTP. Либо мы можем добавить проверку: если fetch не работает, предложить выбрать файл через input. Но по заданию "открывает указанный в JSON блоке файл" - значит автоматически.

Уточним: JSON блок в HTML, например, в виде переменной или в теге <script>. Мы его будем считывать. Настраиваемые параметры: путь к файлу Excel и список листов. Удобно сделать редактируемый JSON через textarea или просто как константу в скрипте. Для простоты сделаем как конфиг в JS, но пользователь может изменить. Можно сделать поле для ввода JSON и кнопку "Загрузить". Но в задании сказано: "В файле service_foto.html задается список (JSON) папок \ листов excel." Это можно интерпретировать как жестко заданный в HTML, но лучше сделать редактируемым.

Я реализую интерфейс:

Textarea для ввода JSON конфигурации (с предустановленным значением: { "file": "tree.xlsx", "sheets": ["foto_person", "foto_family", "foto_group", "foto_location"] }).

Кнопка "Проверить", которая запускает проверку.

Область для вывода отчета с предупреждениями.

Алгоритм:

Прочитать JSON из textarea, разобрать, получить имя файла и список листов.

Загрузить файл Excel через fetch (предполагается, что файл лежит в том же каталоге, что и HTML). Обработать ошибки загрузки.

Для каждого листа из списка:

Проверить, существует ли лист в книге. Если нет - предупреждение.

Считать данные листа. Предполагаем, что есть колонка idA (может быть заголовком). Получим все значения idA.

Также получить список файлов в соответствующей папке. Но для получения списка файлов на клиенте без серверного кода невозможно напрямую прочитать содержимое папки. Это ключевая проблема. В браузере нет API для чтения директорий на сервере или локально без участия пользователя. Задание, видимо, предполагает, что папки существуют на сервере (GitHub Pages) и доступны по HTTP. Но получить список файлов в папке через HTTP без индекса или специального API нельзя. Обычно для таких задач нужен серверный код, который сканирует папки. Но тут говорится о браузерном JS. Возможно, подразумевается, что мы можем получить список файлов из того же Excel, но это не так.

Давайте перечитаем: "С указанного в списке листа, например, foto_person, считывается значение поля idA – имя файла. Если в одноименной с названием листа папке (например, \foto_person) нет указанного файла (как значение в поле idA), то дается соответствующее предупреждение (в итоговом отчете по всем папкам \ листам). Если в данной папке есть другие файлы, не указанные в поле idA для данной папки (имя листа совпадает с именем папки), то также видается предупреждение."

Для проверки наличия других файлов, не указанных в idA, нам нужен полный список файлов в папке. В браузере это невозможно без серверного скрипта, который возвращает список. Возможно, задание предполагает, что мы работаем с локальными файлами через File System Access API, но это только в современных браузерах и требует выбора папки пользователем. Или же подразумевается, что файлы лежат на сервере и мы можем получить список, если сервер генерирует листинг. Но GitHub Pages не отдает листинг директорий.

Может быть, есть другой подход: мы можем прочитать все файлы из папки, если они явно перечислены где-то ещё (например, в другом листе Excel). Но это не указано.

Возможно, задание имеет в виду, что папки физически существуют на сервере, и мы можем делать запросы к каждому предполагаемому файлу (HEAD запрос) и по ответу определять, есть ли файл. Но для обнаружения "других файлов" это не поможет, так как мы не знаем их имена.

Тут противоречие. Вероятно, предполагается, что пользователь сам укажет список файлов в папке (например, через JSON). Но в задании нет такого.

Другой вариант: мы можем использовать серверный язык (PHP, Python), но задание требует браузерный JS.

Может быть, я неправильно понял: "Составить файл service_foto.html. В файле service_foto.html задается список (JSON) папок \ листов excel." Это значит, что в JSON мы задаём соответствие между папками и листами. Далее: "Программа открывает указанный в JSON блоке файл (из текущего каталога) и делает проверку: С указанного в списке листа, например, foto_person, считывается значение поля idA – имя файла. Если в одноименной с названием листа папке (например, \foto_person) нет указанного файла (как значение в поле idA), то дается соответствующее предупреждение. Если в данной папке есть другие файлы, не указанные в поле idA для данной папки (имя листа совпадает с именем папки), то также видается предупреждение."

Для проверки наличия других файлов в папке нужно знать, какие файлы там есть. Эту информацию можно получить, только если папка доступна для листинга. GitHub Pages не предоставляет листинг. Локально через file:// мы могли бы использовать File API с <input type="file" webkitdirectory> для выбора папки, но это требует действий пользователя и не соответствует автоматическому режиму.

Я думаю, что вероятно, задание упрощено и подразумевает, что файлы находятся в известных местах, и мы можем проверить существование конкретных файлов (по idA), а для проверки "других файлов" мы можем считать, что других файлов быть не должно, и если есть, то это ошибка, но мы не можем это проверить. Возможно, имеется в виду, что в папке должны быть только файлы, перечисленные в idA, и никаких лишних, но как узнать о лишних? Либо пользователь должен вручную убедиться, либо есть дополнительный источник.

Может быть, можно использовать метаданные из того же Excel: если на листе есть не только idA, но и другие колонки, но это не даст список файлов.

Я склоняюсь к тому, что задание сформулировано некорректно для клиентского JS, если нужна проверка наличия других файлов. Но, возможно, подразумевается, что все файлы, которые должны быть в папке, перечислены в листе (idA), и если в папке есть другие, то это выявляется сравнением с тем, что мы можем получить, запросив каждый файл из предполагаемого списка? Но чтобы узнать о существовании других, нужно знать их имена.

Может быть, мы можем получить список файлов из самого Excel, если там есть перечисление всех файлов? Но не указано.

Вероятно, задача была дана без учета технических ограничений, и ожидается, что мы реализуем только первую часть (проверка наличия файлов по idA), а вторую опустим, либо сделаем заглушку. Но я постараюсь придумать обходной путь: можно предположить, что в той же директории, что и HTML, лежит папка с фотографиями, и мы можем попытаться получить список файлов, если сервер поддерживает листинг. Но это не гарантировано.

Альтернативно, можно использовать сервис, который возвращает список файлов, например, через GitHub API, если репозиторий публичный. Но это слишком специфично.

Возможно, правильнее реализовать проверку только наличия файлов из idA, а сообщение о лишних файлах выводить как "не реализовано". Но задание явно требует оба предупреждения.

Другой подход: использовать IndexedDB или локальное хранилище для списка файлов, но это не подходит.

Я думаю, нужно признать, что без серверной части невозможно надежно получить список файлов в папке. Поэтому я реализую только проверку существования файлов из idA, а для проверки лишних файлов предложу пользователю вручную проверить или ввести список файлов в папке через дополнительный JSON. Но это будет усложнением.

Может быть, подразумевается, что в Excel есть еще один лист, который содержит список всех файлов в папке? Например, лист с именем "files_foto_person" или что-то подобное. Но не сказано.

Учитывая, что задание дано в учебных целях, вероятно, ожидается, что мы будем использовать XLSX для чтения данных, а для работы с файловой системой - имитацию. Например, мы можем создать в JSON блоке ещё одно поле, которое содержит список файлов в каждой папке. Тогда программа сможет сравнить. Но это не соответствует "текущему каталогу".

Давайте подумаем: если мы откроем HTML на локальном компьютере через file://, мы можем использовать File API для чтения содержимого папки, выбрав папку пользователем. Это возможно с атрибутом webkitdirectory. Мы можем добавить кнопку "Выбрать папку foto_person" и так для каждой, но это сильно усложнит интерфейс. Однако это может быть приемлемо для локальной проверки, а на GitHub Pages это не сработает (webkitdirectory не будет работать с серверными путями).

Таким образом, универсального решения нет.

Возможно, составитель задания предполагал, что мы будем использовать серверный язык, но ошибся. Я все же попробую реализовать максимально приближенно: будем считать, что файлы лежат в тех же папках относительно корня сайта, и мы можем проверить существование каждого файла через fetch с методом HEAD. А для проверки "других файлов" сделаем допущение, что мы можем получить список файлов, если в папке есть файл .listing или мы можем загрузить предопределенный JSON-файл с именами. Но это не описано.

Поэтому я, вероятно, реализую только первую часть, а вторую часть пометю как "невозможно проверить в браузере без серверной поддержки". Но заказчик может быть недоволен.

Давайте поищем в интернете похожие задачи: часто для проверки наличия файлов используют отправку HEAD запросов. А для обнаружения лишних файлов — это невозможно без листинга. Но можно предположить, что все файлы в папке известны из Excel, и если какие-то файлы отсутствуют в idA, но есть в папке, мы не узнаем. Так что только односторонняя проверка.

Я добавлю в отчет информацию о том, что проверка наличия лишних файлов требует доступа к списку файлов в папке, который невозможно получить автоматически. Возможно, пользователь может предоставить список через дополнительный файл.

Учитывая всё вышесказанное, я создам HTML, который:

Позволяет задать JSON конфиг (путь к Excel и массив листов).

Загружает Excel.

Для каждого листа собирает значения idA (предполагаем, что первая строка - заголовки, ищем колонку "idA").

Для каждого значения idA проверяет существование файла в папке с именем листа (например, "./foto_person/значение_idA"). Использует fetch с HEAD.

Формирует отчет: для каждой папки список отсутствующих файлов, а также (если бы мы могли получить список файлов) - лишние файлы. Для лишних файлов выведем сообщение, что проверка не выполнена из-за отсутствия доступа к листингу папки.

Такой подход будет честным.

Теперь детали:

Используем библиотеку XLSX для чтения Excel.

Функция загрузки файла: fetch(url) возвращает ArrayBuffer, затем XLSX.read.

Для каждого листа получаем данные через XLSX.utils.sheet_to_json, что даст массив объектов с ключами из заголовков. Если заголовок "idA" есть, собираем все значения idA (не пустые). Предполагаем, что idA содержит имя файла (может быть с расширением? Без расширения? Наверное, полное имя файла, например, "photo1.jpg".)

Для каждого такого имени проверяем существование: fetch(путь_к_папке + имя_файла, {method: 'HEAD'}). По ответу (ok или статус 404) определяем наличие.

Собираем отсутствующие файлы.

Для лишних файлов - пропускаем.

Важно: пути к папкам должны быть относительно расположения HTML. На GitHub Pages это будет работать, если папки существуют. На локальной машине при file:// fetch на файлы может не работать из-за CORS, но если все файлы локальные, то file:// запросы к файлам должны работать (например, fetch('file:///...')), но современные браузеры ограничивают такие запросы из соображений безопасности. Обычно нельзя делать fetch из file:// в документе, загруженном через file://, если не включены определенные настройки. Это проблема. Но можно использовать XMLHttpRequest, но тоже не поможет. Решение: запускать локальный сервер.

Таким образом, на локальной машине проще всего открыть HTML через локальный сервер. В инструкции можно написать "для локального тестирования используйте локальный сервер (например, python -m http.server)".

Теперь напишем код.

Структура:

index.html (service_foto.html)

Внутри скрипт с функциями.

Элементы: textarea для JSON (с предзаполненным значением), кнопка "Запустить проверку", div для вывода отчета.

JSON конфиг:
{
"excelFile": "tree.xlsx",
"sheets": ["foto_person", "foto_family", "foto_group", "foto_location"]
}

Дополнительно можно указать поле idColumn (по умолчанию "idA"), путь к папкам (относительный), но по условию имя папки совпадает с именем листа, так что просто используем лист как имя подпапки.

Обработка:

При клике на кнопку парсим JSON, проверяем валидность.

Загружаем Excel.

Для каждого листа обрабатываем.

Отчет выводим в HTML.

Добавим индикацию загрузки.

Также учтем, что лист может не содержать колонку idA, тогда предупреждение.
