# config.json — параметры конфигурации семейного дерева
# Все параметры можно изменять вручную в config.json.

# --- Папка с миниатюрами (фото персон) ---

picDir      — путь к папке с фото персон (по умолчанию: "pic")
              Можно указывать как относительный путь (например "pic", "assets/photos"),
              так и полный абсолютный путь (например "/home/user/photos").
              Значение по умолчанию: "pic" (папка pic рядом с index.html).

picDirType  — режим работы с папкой фото (по умолчанию: "relative")
              Три режима:

                "relative"              — только относительный путь picDir.
                                         Работает при запуске через HTTP-сервер
                                         (GitHub Pages, локальный веб-сервер).
                                         Фото отображаются локально, но НЕ отображаются
                                         в GraphvizOnline (сайт не может загрузить
                                         локальные относительные пути).
                                         Пример:
                                           "picDir": "pic"
                                           "picDirType": "relative"

                "relativeGraphvizOnline" — запуск через HTTP-сервер (GitHub Pages).
                                         Локальное отображение использует picDir
                                         (относительный путь).
                                         В панели «Код DOT» и в ссылке на GraphvizOnline
                                         подставляется picDirGraphvizOnline (полный URL).
                                         Важно: dreampuf.github.io/GraphvizOnline НЕ
                                         отображает фото по URL — это ограничение сервиса
                                         (браузерная версия Graphviz не умеет загружать
                                         изображения по HTTP). DOT-код с полными URL
                                         полезен для локального Graphviz, VS Code и
                                         других инструментов, поддерживающих внешние URL.
                                         Пример:
                                           "picDir": "pic"
                                           "picDirType": "relativeGraphvizOnline"
                                           "picDirGraphvizOnline": "https://bpmbpm.github.io/family-tree/ver1/pic"

                "global"                — запуск с локального диска через file://.
                                         Использует picDirGlobal как полный
                                         абсолютный путь к папке с фото.
                                         Пример для Windows:
                                           "picDir": "pic"
                                           "picDirType": "global"
                                           "picDirGlobal": "C:/Users/YourName/Desktop/ver1/pic"
                                         Пример для Linux/Mac:
                                           "picDir": "pic"
                                           "picDirType": "global"
                                           "picDirGlobal": "/home/user/family-tree/ver1/pic"

picDirGlobal — полный абсолютный путь к папке с фото; используется в режиме "global"
              Пример для Windows: "C:/Users/YourName/Desktop/ver1/pic"
              Пример для Linux/Mac: "/home/user/family-tree/ver1/pic"
              Значение по умолчанию: "" (не задано).

picDirGraphvizOnline — URL к папке с фото для GraphvizOnline; используется в режиме "relativeGraphvizOnline"
              Пример: "https://bpmbpm.github.io/family-tree/ver1/pic"
              Значение по умолчанию: "" (не задано).
              Примечание: GraphvizOnline (dreampuf.github.io) — это веб-приложение, которое
              рендерит граф прямо в браузере. Из-за ограничений безопасности браузера
              изображения по HTTP-ссылкам (image="https://...") в самом GraphvizOnline
              НЕ отображаются — это ограничение самого сервиса GraphvizOnline, а не нашего кода.
              Тем не менее, абсолютный URL в DOT-коде сохраняется корректно и может быть
              использован в других инструментах Graphviz (локальном dot, VS Code и т.д.).

# --- Размеры узлов (в дюймах, при 72 DPI) ---

nodeWidth   — ширина узла в дюймах (по умолчанию: 1.1)
nodeHeight  — высота узла в дюймах (по умолчанию: 1.9)

# --- Шрифт ---

fontName    — название шрифта для подписей узлов (по умолчанию: "Arial")
              Graphviz поддерживает системные шрифты: Arial, Times New Roman, Courier New и др.
fontSize    — размер шрифта в пунктах (по умолчанию: 11)
lineSpacing — межстрочный интервал (по умолчанию: 1.0; значение 1.5 = полуторный интервал)
lineSpacingSurName2 — масштаб размера шрифта для узлов, у которых заполнено поле surName2 (по умолчанию: 0.8).
              Размер шрифта = fontSize × lineSpacingSurName2.
              Меньшее значение → меньший шрифт → более компактное отображение.
              Пример: fontSize=11, lineSpacingSurName2=0.8 → шрифт 8.8pt для таких узлов.

# --- Цвета узлов ---

maleColor   — цвет заливки узла для мужчин
              Значение: "lightsteelblue"  (светло-стальной синий)
              HEX-код:  #a3c4f3

femaleColor — цвет заливки узла для женщин
              Значение: "lightpink"        (светло-розовый)
              HEX-код:  #fbb9c0

unknownColor — цвет заливки узла, если пол не указан
              Значение: "lightgrey"        (светло-серый)
              HEX-код:  #d5d8dc

borderColor — цвет рамки (границы) узла
              Значение: "darkslategray"    (тёмно-серый сланец)
              HEX-код:  #2c3e50

edgeColor   — цвет стрелок и линий связей
              Значение: "slategray"        (серый сланец)
              HEX-код:  #5d6d7e

# --- Примечания ---
# Цвета можно задавать:
#   - именем (например "lightblue", "pink") — список имён: https://graphviz.org/doc/info/colors.html
#   - HEX-кодом (например "#a3c4f3")
# lineSpacingSurName2 применяется как множитель для fontSize узлов с заполненным surName2.
